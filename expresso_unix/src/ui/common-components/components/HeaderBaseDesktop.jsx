import "../styles/HeaderBaseDesktop.css"

export default function HeaderBaseDesktop({headerTitle}){
    return(
        <div id="header-base-desktop">
            <h1>{headerTitle}</h1>
        </div>
    )
}